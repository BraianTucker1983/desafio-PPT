{
  "name": "desafioppt",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --dist-dir build --public-url ./desafio-PPT",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "parcel": "^2.16.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
